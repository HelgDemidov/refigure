# Dev/CI tooling. Install: uv pip install -r requirements-dev.txt
# Checks (from repo root; config in pyproject.toml):
#   ruff check .
#   mypy refigure
#   pytest --cov --cov-report=term-missing
-r requirements.txt
ruff==0.16.5
mypy==2.3.1
pytest==9.1.1
pytest-cov==7.1.0
types-openpyxl==3.1.5.20260827
# mermaidx — optional render-validation layer inside chart_render.py (try/except
# ImportError + capability flag, not a pip extra — same pattern already proven
# in the source pipeline). Installed here so the dev/test environment covers that code path;
# NOT declared as a hard dependency of any published extra.
mermaidx==0.9.4
hypothesis==6.167.0
# mcp — pulled in for the dev/test environment same as mermaidx above (not
# a hard dependency of any extra except [mcp] itself); pins anyio/starlette/
# uvicorn transitively.
mcp==2.1.1
# httpx — tests/unit/mcp/test_http.py drives the MCP HTTP transport in-process
# via httpx.AsyncClient/httpx.ASGITransport. Not a hard dependency of any
# extra (mcp>=2.0 itself already depends on httpx2, not this) — until
# anthropic 1.0.0 it arrived only by transitive accident (anthropic==0.122.0
# required classic httpx<1); anthropic 1.0.0 switched to httpx2 too, so this
# needs to be declared explicitly now instead of relying on that.
httpx==0.28.1
